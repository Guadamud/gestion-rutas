# Backend Environment Variables

# Puerto del servidor
PORT=5000

# Base de datos PostgreSQL (Desarrollo Local)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=gestion_rutas
DB_USER=postgres
DB_PASSWORD=tu_password_aqui

# Base de datos PostgreSQL (Producción - Render)
# Render proporciona DATABASE_URL automáticamente
# DATABASE_URL=postgresql://user:password@host:port/database

# JWT Secret
JWT_SECRET=tu_clave_secreta_jwt_muy_segura_aqui_cambiar_en_produccion

# Node Environment
NODE_ENV=development

# Frontend URL para CORS (Producción)
# FRONTEND_URL=https://tu-proyecto.vercel.app
