const { sequelize } = require('./config/database'); const Frecuencia = require('./models/Frecuencia'); async function syncFrecuencias() { try { console.log('Sincronizando modelo de Frecuencias...'); await Frecuencia.sync({ alter: true }); console.log('Columna conductorId agregada'); process.exit(0); } catch (error) { console.error('Error:', error); process.exit(1); } } syncFrecuencias();
